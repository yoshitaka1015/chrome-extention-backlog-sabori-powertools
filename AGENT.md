# 開発時の運用メモ

- コードを修正したあとは、必ず `npm run build` を実行し最新の成果物 (`dist/`) を生成してください。Chrome 拡張の再読み込み前にビルド済みであることを確認します。
- コードを修正した後は、必ず `git add .`と`git commit -m "{適切な日本語コメント}"`を実施し、変更を記録に残してください。 
- 今後、Codex との会話は日本語で行ってください。
